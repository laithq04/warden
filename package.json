{
  "name": "warden",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n client -c cyan \"npm run dev --workspace packages/client\"",
    "build": "npm run build --workspace packages/client"
  },
  "devDependencies": {
    "concurrently": "10.0.3"
  }
}
